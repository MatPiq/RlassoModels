[build-system]
requires = [
  "setuptools>=42",
  "wheel",
  "pybind11",
  "scikit-build",
  "requests",
  "setuptools_scm",
]

build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
