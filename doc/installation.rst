#####################################
Installation
#####################################

`rlassopy` can be installed using pip::

    $ pip install rlassopy 

or conda-forge::

    $ conda install -c conda-forge rlassopy
